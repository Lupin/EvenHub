{
  "name": "pixel-draw",
  "version": "0.1.0",
  "description": "Reproduction de l'interface G2 avec grand texte central (image rendue)",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "generate-image": "python3 scripts/generate-large-text.py"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "0.0.10"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
